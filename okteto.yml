build:
  hello-world:
    image: okteto.dev/java-maven-hello-world:1.0.0
    context: .
deploy:
  - kubectl apply -f k8s.yml
dev:
  hello-world:
    command: mvn spring-boot:run
    sync:
      - .:/app
    forward:
      - 5005:5005
